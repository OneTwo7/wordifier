<%= form_for(@comment, url: url, html: {class: "form"}, remote: true) do |f| %>
  <%= render "shared/error_messages", object: @comment %>

  <%= f.text_area :content, class: "form-control" %>

  <%= f.submit submit_text, class: "btn btn-primary" %>

<% end %>
